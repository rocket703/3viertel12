---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Über uns" currentPage="about">
  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap" rel="stylesheet">

  <section class="about-hero">
    <div class="noise"></div>
    <div class="container">
      
      <div class="brand-header">
        <h1 class="band-name">
          <span class="name-top red-block">3/4</span>
          <span class="name-bottom white-rock-style">DIE STORY</span>
        </h1>
      </div>

      <div class="intro-grid">
        <div class="image-box">
          <div class="image-wrapper">
  <img 
    src="https://pixabay.com/de/images/download/gronlandgorilla-ai-generated-8628431_1920.png" 
    alt="3/4Zwööölwe Rehearsal" 
    class="img-base" 
  />
  
  <img 
    src="https://images.unsplash.com/photo-1470229722913-7c0e2dbbafd3?q=80&w=1000&auto=format&fit=crop" 
    alt="3/4Zwööölwe Live Action" 
    class="img-slide" 
  />
  <div class="image-frame"></div>
</div>
        </div>
        <div class="text-box">
          <h2 class="section-title">LAUT SEIT TAG EINS</h2>
          <p class="snappy-para">
            3/4ZWÖÖÖLWE entstand nicht am Reißbrett, sondern im feuchten Keller zwischen leeren Bierkästen und durchgebrannten Röhrenamps. 
          </p>
          <p class="snappy-para">
            Wir spielen keinen Rock – wir leben ihn. Jedes Riff ist eine Ansage, jeder Schlag ein Statement. Wer uns sucht, findet uns da, wo es wehtut: Vor der PA, mitten im Moshpit.
          </p>
        </div>
      </div>

      <div class="members-section">
        <h2 class="white-rock-style small-heading">DIE BESATZUNG</h2>
        
        <div class="members-grid">
          <div class="member-card" onclick="this.classList.toggle('open')">
            <div class="card-inner">
              <div class="member-photo">
                <div class="photo-overlay">DETAILS +</div>
                <img src="https://images.unsplash.com/photo-1511671782779-c97d3d27a1d4?w=400" alt="Vocals" />
              </div>
              <div class="member-info">
                <h3>STIMME / AXE</h3>
                <div class="details">
                  <p>RAUCHIGE STIMME, HARTE RIFFS. MAG KAFFEE SCHWARZ UND VERSTÄRKER AUF 11.</p>
                </div>
              </div>
            </div>
          </div>

          <div class="member-card" onclick="this.classList.toggle('open')">
            <div class="card-inner">
              <div class="member-photo">
                <div class="photo-overlay">DETAILS +</div>
                <img src="https://images.unsplash.com/photo-1564186763535-ebb21ef5277f?w=400" alt="Bass" />
              </div>
              <div class="member-info">
                <h3>TIEFTÖNER</h3>
                <div class="details">
                  <p>DAS FUNDAMENT. BRINGT DIE WÄNDE ZUM WACKELN. BASS IST SEINE RELIGION.</p>
                </div>
              </div>
            </div>
          </div>

          <div class="member-card" onclick="this.classList.toggle('open')">
            <div class="card-inner">
              <div class="member-photo">
                <div class="photo-overlay">DETAILS +</div>
                <img src="https://images.unsplash.com/photo-1519892300165-cb5542fb47c7?w=400" alt="Drums" />
              </div>
              <div class="member-info">
                <h3>SCHLAGWERK</h3>
                <div class="details">
                  <p>DER PULS DES WAHNSINNS. ZERSTÖRT STICKS ZUM FRÜHSTÜCK.</p>
                </div>
              </div>
            </div>
          </div>

          <div class="member-card" onclick="this.classList.toggle('open')">
            <div class="card-inner">
              <div class="member-photo">
                <div class="photo-overlay">DETAILS +</div>
                <img src="https://images.unsplash.com/photo-1525201548942-d8b8bb66ec70?w=400" alt="Lead Guitar" />
              </div>
              <div class="member-info">
                <h3>LEAD-TERROR</h3>
                <div class="details">
                  <p>SOLOS AUS DER HÖLLE. SCHNELLER ALS DAS LICHT, LAUTER ALS DER REST.</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="cta-bottom">
        <a href="/3viertel12" class="block-font">ZURÜCK</a>
      </div>

    </div>
  </section>
</Layout>

<style>
  /* --- BASE & HERO --- */
  .about-hero {
    background: #050505;
    padding: 6rem 1rem;
    position: relative;
    overflow: hidden;
    min-height: 100vh;
  }

  .container { max-width: 1100px; margin: 0 auto; position: relative; z-index: 10; }

  /* Branding Header */
  .brand-header { margin-bottom: 5rem; text-align: center; }
  .band-name { position: relative; display: inline-block; transform: rotate(-3deg); font-family: 'Bebas Neue', sans-serif; }
  .red-block { position: absolute; top: -20%; left: -10%; color: #cc0000; font-size: clamp(3rem, 10vw, 6rem); z-index: 5; }
  .white-rock-style { display: block; color: #fff; font-size: clamp(4rem, 15vw, 10rem); text-shadow: 4px 4px 0px #cc0000; letter-spacing: -2px; }

  /* --- IMAGE SLIDE EFFECT --- */
  .intro-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    margin-bottom: 8rem;
  }

  .image-box { position: relative; }
  .image-wrapper {
    position: relative;
    width: 100%;
    aspect-ratio: 4/3;
    overflow: hidden;
    border: 2px solid #fff;
    z-index: 2;
  }

  .img-base { width: 100%; height: 100%; object-fit: cover; filter: grayscale(1); }
  .img-slide {
    position: absolute;
    top: 0; left: 0;
    width: 100%; height: 100%;
    object-fit: cover;
    transform: translateX(100%); /* Startet rechts außerhalb */
    transition: transform 0.6s cubic-bezier(0.77, 0, 0.175, 1);
    z-index: 3;
  }

  .image-box:hover .img-slide { transform: translateX(0); }

  .image-frame {
    position: absolute;
    top: 15px; left: -15px;
    width: 100%; height: 100%;
    border: 3px solid #cc0000;
    z-index: 1;
  }

  /* Intro Text */
  .section-title { font-family: 'Bebas Neue', sans-serif; font-size: 3rem; color: #fff; margin-bottom: 1.5rem; }
  .snappy-para { color: #888; font-size: 1.2rem; line-height: 1.6; font-weight: 700; text-transform: uppercase; margin-bottom: 1rem; }

  /* --- MEMBER CARDS --- */
  .members-section { margin-bottom: 6rem; }
  .small-heading { font-size: clamp(2.5rem, 8vw, 4.5rem) !important; text-align: left; margin-bottom: 3rem; }

  .members-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1.5rem;
  }

  .member-card {
    background: #111;
    cursor: pointer;
    border: 1px solid #222;
    transition: all 0.3s ease;
    overflow: hidden;
    position: relative;
  }

  .member-card:hover { border-color: #cc0000; transform: translateY(-5px); }

  .member-photo {
    position: relative;
    width: 100%;
    aspect-ratio: 1/1;
    overflow: hidden;
  }

  .member-photo img { width: 100%; height: 100%; object-fit: cover; filter: grayscale(1); transition: 0.4s; }
  .photo-overlay {
    position: absolute; inset: 0; background: rgba(204, 0, 0, 0.6);
    display: flex; align-items: center; justify-content: center;
    color: #fff; font-family: 'Bebas Neue', sans-serif; font-size: 1.5rem;
    opacity: 0; transition: 0.3s; z-index: 2;
  }

  .member-card:hover .photo-overlay { opacity: 1; }
  .member-card:hover img { transform: scale(1.1); filter: grayscale(0); }

  .member-info { padding: 1.5rem; text-align: center; }
  .member-info h3 { font-family: 'Bebas Neue', sans-serif; color: #fff; font-size: 1.8rem; margin: 0; letter-spacing: 1px; }

  /* Die versteckten Details */
  .details {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.4s ease-out, opacity 0.3s;
    opacity: 0;
  }

  .details p { color: #cc0000; font-weight: 800; font-size: 0.9rem; margin-top: 1rem; text-transform: uppercase; }

  /* Klick-Zustand */
  .member-card.open { border-color: #fff; background: #1a1a1a; }
  .member-card.open .details { max-height: 200px; opacity: 1; }
  .member-card.open .photo-overlay { display: none; }

  /* --- CTA & NOISE --- */
  .cta-bottom { text-align: center; margin-top: 5rem; }
  .block-font {
    display: inline-block; background: #cc0000; color: #fff; text-decoration: none;
    font-family: 'Bebas Neue', sans-serif; font-size: 1.5rem; padding: 1.2rem 3rem;
    clip-path: polygon(5% 0, 100% 0, 95% 100%, 0% 100%);
  }

  .noise { position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: url('https://www.transparenttextures.com/patterns/asfalt-dark.png'); opacity: 0.2; pointer-events: none; }

  /* --- RESPONSIVE --- */
  @media (max-width: 768px) {
    .intro-grid { grid-template-columns: 1fr; gap: 2rem; }
    .text-box { text-align: center; }
    .small-heading { text-align: center; }
    .image-frame { display: none; }
  }
</style>